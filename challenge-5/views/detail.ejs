<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Binar Car Rental</title>
    <link rel="stylesheet" type="text/css" href="/node_modules/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body class="bg-body-secondary">
    <div class="w-100">
        <div class="w-100">
            <%- include('utils/navbar') %>
        </div>
        <div class="container w-100">
            <h1 class="fw-bold fs-3 my-4">Car Detail</h1>
            <div class="p-4 shadow-sm d-flex flex-row w-100 bg-white">
                <div class="car-image mb-4 w-100">
                    <img src="<%= car.image %>" alt="<%= car.name %>" class="w-50 h-100">
                </div>
                <div class="mb-3">
                    <h2 class="fw-bold"><%= car.name %></h2>
                    <p class="text-muted"><%= car.category %></p>
                    <p class="fw-bold fs-5"><%= car.price %> / hari</p>
                    <p><i class="bi bi-key-fill me-2"></i>Start rent - Finish rent</p>
                    <p><i class="bi bi-clock me-2"></i>Updated at <%= car.updatedAt %></p>
                </div>
            </div>
        </div>
    </div>
</body>

</html>