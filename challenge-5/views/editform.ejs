<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Binar Car Rental</title>
    <link rel="stylesheet" type="text/css" href="/node_modules/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
</head>

<body class="bg-body-secondary">
    <div class="w-100">
        <div class="w-100">
            <%- include('utils/navbar') %>
        </div>
        <div class="container">
            <h1 class="fw-bold fs-3 my-4">Edit Form</h1>
            <div class="bg-white">
                <form action="/cars/edit/<%=car.id%>?_method=put" method="post" enctype="multipart/form-data" class="d-flex flex-column w-50">
                    <div class="mb-3 d-flex justify-content-between mx-3">
                        <label for="name">Name</label>
                        <input type="text" name="name" id="name" class="w-50" value="<%=car.name%>">
                    </div>
                    <div class="mb-3 d-flex justify-content-between mx-3">
                        <label for="category">Category</label>
                        <input type="text" name="category" id="category" class="w-50" value="<%=car.category%>">
                    </div>
                    <div class="mb-3 d-flex justify-content-between mx-3">
                        <label for="price">Price</label>
                        <input type="number" name="price" id="price" class="w-50" value="<%=car.price%>">
                    </div>
                    <div class="mb-3 d-flex justify-content-between mx-3">
                        <label for="status">Status</label>
                        <select name="status" id="status">
                            <option value="" disabled selected>Pilih Status</option>
                            <option value="AVAILABLE">Available</option>
                            <option value="RENTED">Rented</option>
                        </select>
                    </div>
                    <div class="mb-3 d-flex justify-content-between mx-3">
                        <label for="image">Image</label>
                        <input type="file" name="image" id="image" class="w-50">
                    </div>
                    <button type="submit">Submit</button>
                </form>
            </div>
        </div>
    </div>
    <script src="/node_modules/@popperjs/core/dist/umd/popper.min.js"></script>
    <script src="/node_modules/bootstrap/dist/js/bootstrap.min.js"></script>
</body>

</html>